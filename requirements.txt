# Core dependencies for proxy + API server
flask>=3.0.0
flask-cors>=4.0.0
cryptography>=42.0.0

# Optional: only needed if using the legacy tkinter tray GUI (gui/app.py)
# pystray>=0.19.0
# pillow>=10.0.0
